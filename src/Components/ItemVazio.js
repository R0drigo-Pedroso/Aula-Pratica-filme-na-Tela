import { StyleSheet, Text, View } from "react-native";
import React from "react";

const itemVazio = () => {
  return (
    <View>
      <Text>Não localizamos o filme escolhido!</Text>
    </View>
  );
};

export default itemVazio;

const styles = StyleSheet.create({});
